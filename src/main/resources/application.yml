server:
  port: 8083
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/study
    username: root
    password: root
  mvc:
    view:
      prefix: /view/
      suffix: .html
  jpa:
    hibernate:
      use-new-id-generator-mappings: false
logging:
  level:
    root: info